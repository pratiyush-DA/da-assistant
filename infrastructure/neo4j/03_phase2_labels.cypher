// Phase 2 — SQL Lineage (not used in Phase 1)
// (:SqlTable { id, name, schema, created_at })
// (:SqlProcedure { id, name, body_summary, embedding, line_count, last_modified })
// (:SqlPackage), (:SqlView), (:SqlFunction)
// Relationships: INSERTS_INTO, UPDATES, MERGES, READS_FROM, CREATES, CALLS, BELONGS_TO
